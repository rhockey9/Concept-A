export class NHLCurrentRecord {
    wins: number;
    losses: number;
    otLosses: number;
    points: number;
    streak: string;
    teamAbbreviation: string;

    // constructor(values: Object = {}) {
    //     Object.assign(this, values);
    //   }
}